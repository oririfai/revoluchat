global:
  scrape_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "revoluchat"
    static_configs:
      - targets: ["host.docker.internal:4000"]
    metrics_path: "/health/metrics"
